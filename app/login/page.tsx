"use client"; import { useState } from "react"; import { useRouter } from "next/navigation"; export default function LoginPage() { const router = useRouter(); const [aba, setAba] = useState("login"); const [form, setForm] = useState({ nome: "", email: "", senha: "", negocio: "", whatsapp: "" }); const [loading, setLoading] = useState(false); const [erro, setErro] = useState(""); async function handleLogin(e: React.FormEvent) { e.preventDefault(); setLoading(true); setErro(""); try { const res = await fetch("/api/auth/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email: form.email, senha: form.senha }) }); const data = await res.json(); if (!res.ok) { setErro(data.error ?? "Erro ao entrar"); return; } router.push("/dashboard"); } finally { setLoading(false); } } async function handleCadastro(e: React.FormEvent) { e.preventDefault(); setLoading(true); setErro(""); try { const res = await fetch("/api/auth/cadastro", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); const data = await res.json(); if (!res.ok) { setErro(data.error ?? "Erro ao cadastrar"); return; } router.push("/dashboard"); } finally { setLoading(false); } } const inp: React.CSSProperties = { width: "100%", border: "1px solid #E5E7EB", borderRadius: 8, padding: "10px 12px", fontSize: 14, outline: "none", boxSizing: "border-box" }; return (<div style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", background: "#F7F8FA", fontFamily: "system-ui, sans-serif" }}><div style={{ background: "#fff", borderRadius: 16, border: "1px solid #E5E7EB", padding: 32, width: "100%", maxWidth: 400, margin: "0 16px" }}><div style={{ textAlign: "center", marginBottom: 28 }}><div style={{ fontSize: 32, marginBottom: 8 }}>📅</div><div style={{ fontSize: 20, fontWeight: 700, color: "#111" }}>AgendaFacil</div><div style={{ fontSize: 13, color: "#6B7280", marginTop: 4 }}>Confirmacao automatica pelo WhatsApp</div></div><div style={{ display: "flex", background: "#F3F4F6", borderRadius: 10, padding: 4, marginBottom: 24 }}>{["login", "cadastro"].map((a) => (<button key={a} onClick={() => { setAba(a); setErro(""); }} style={{ flex: 1, padding: "8px", borderRadius: 8, border: "none", cursor: "pointer", fontSize: 14, fontWeight: aba === a ? 600 : 400, background: aba === a ? "#fff" : "transparent", color: aba === a ? "#111" : "#6B7280" }}>{a === "login" ? "Entrar" : "Criar conta"}</button>))}</div>{aba === "login" ? (<form onSubmit={handleLogin} style={{ display: "flex", flexDirection: "column", gap: 14 }}><div><label style={{ fontSize: 12, color: "#6B7280", display: "block", marginBottom: 4 }}>Email</label><input type="email" required value={form.email} onChange={e => setForm({...form, email: e.target.value})} placeholder="seu@email.com" style={inp} /></div><div><label style={{ fontSize: 12, color: "#6B7280", display: "block", marginBottom: 4 }}>Senha</label><input type="password" required value={form.senha} onChange={e => setForm({...form, senha: e.target.value})} placeholder="••••••••" style={inp} /></div>{erro && <p style={{ fontSize: 12, color: "#DC2626", background: "#FEF2F2", padding: "8px 12px", borderRadius: 8 }}>{erro}</p>}<button type="submit" disabled={loading} style={{ background: "#4338CA", color: "#fff", border: "none", borderRadius: 10, padding: 12, fontSize: 15, fontWeight: 600, cursor: "pointer" }}>{loading ? "Entrando..." : "Entrar"}</button></form>) : (<form onSubmit={handleCadastro} style={{ display: "flex", flexDirection: "column", gap: 14 }}><div><label style={{ fontSize: 12, color: "#6B7280", display: "block", marginBottom: 4 }}>Nome do negocio</label><input required value={form.negocio} onChange={e => setForm({...form, negocio: e.target.value})} placeholder="Salao Beleza e Arte" style={inp} /></div><div><label style={{ fontSize: 12, color: "#6B7280", display: "block", marginBottom: 4 }}>Seu nome</label><input required value={form.nome} onChange={e => setForm({...form, nome: e.target.value})} placeholder="Maria Silva" style={inp} /></div><div><label style={{ fontSize: 12, color: "#6B7280", display: "block", marginBottom: 4 }}>WhatsApp do negocio (com DDD)</label><input required value={form.whatsapp} onChange={e => setForm({...form, whatsapp: e.target.value})} placeholder="33999999999" style={inp} /></div><div><label style={{ fontSize: 12, color: "#6B7280", display: "block", marginBottom: 4 }}>Email</label><input type="email" required value={form.email} onChange={e => setForm({...form, email: e.target.value})} placeholder="seu@email.com" style={inp} /></div><div><label style={{ fontSize: 12, color: "#6B7280", display: "block", marginBottom: 4 }}>Senha</label><input type="password" required value={form.senha} onChange={e => setForm({...form, senha: e.target.value})} placeholder="minimo 6 caracteres" style={inp} /></div>{erro && <p style={{ fontSize: 12, color: "#DC2626", background: "#FEF2F2", padding: "8px 12px", borderRadius: 8 }}>{erro}</p>}<button type="submit" disabled={loading} style={{ background: "#4338CA", color: "#fff", border: "none", borderRadius: 10, padding: 12, fontSize: 15, fontWeight: 600, cursor: "pointer" }}>{loading ? "Criando..." : "Criar conta gratis"}</button><p style={{ fontSize: 12, color: "#9CA3AF", textAlign: "center" }}>7 dias gratis - Cancele quando quiser</p></form>)}</div></div>); }